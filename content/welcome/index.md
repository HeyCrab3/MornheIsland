# 资源下发

MornheIsland 支持向绑定的 ClassIsland 教室一键下发各类配置资源：

- **课表**：可视化网格编辑器，拖拽科目即可完成排课，自动生成 ClassIsland 兼容格式
- **时间表**：可拖拽排序的时段管理，支持配置每节课的默认科目
- **课程表（科目库）**：开箱即用 13 门标准科目，UUID 格式，完全兼容 ClassIsland
- **策略配置**：8 项管控开关，支持 JSON 双向同步，禁止编辑课表、锁定设置等

配置完成后，在「组织设置」中生成 `ManagementPreset.json`，复制到目标设备即可自动接入。